package printer;

import interfaces.OutputStream;

public class PrettyPrinter {
  public void print(OutputStream stream, PrettyPrinterConfiguration configuration) {
    if (stream != null) {
      // pretty print the AST to stream;
    }
  }
}
